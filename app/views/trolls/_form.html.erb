<%= form_for [@dev, @troll] do |f| %>
  <% if @troll.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@troll.errors.count, "error") %> prohibited this troll from being saved:</h2>

      <ul>
      <% @troll.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
