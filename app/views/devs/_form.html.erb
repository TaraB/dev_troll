<%= form_for(@dev) do |f| %>
  <% if @dev.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dev.errors.count, "error") %> prohibited this dev from being saved:</h2>

      <ul>
      <% @dev.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
